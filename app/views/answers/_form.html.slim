= form_for( resource.new_record? ? [@question, resource] : resource, html: { class: "form-horizontal form-edit", role: "form" }, remote: (true if controller.controller_name == "questions") ) do |f|
  = render 'common/errors', resource: resource
  .col-xs-12
    .form-group
      = f.label :body, class: "control-label"
      = f.text_area :body, class: "form-control", rows: 5
    .form-group
      = f.submit t('common.save'), class: "btn btn-success btn-sm  pull-right"
